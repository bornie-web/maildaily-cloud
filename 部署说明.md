# 邮件简报：独立云端部署

这是单人自用的 Python/FastAPI 服务，不依赖 ChatGPT、Sites 或 OpenAI API。配套手机 App 只需要你的服务器根地址和连接密钥。默认规则归类与正文摘录，不是 AI 摘要。

## 1. 准备运行环境

需要一个能持续运行 Python 3.12 或 Docker 的云端环境，以及公网 HTTPS 根地址和持久磁盘。定时器在服务进程内运行：休眠、被关闭或零实例缩容时无法按时运行。当前仅支持单进程、单副本，必须保持 `--workers 1`。

Windows 也能本机运行服务用于调试，但手机正式连接和 Google 回调需可访问的 HTTPS 地址。没有服务器时，先用 App 的“先体验新界面”；不要把临时本机演示当成已经上线。

## 2. 创建自己的 Google OAuth 配置

在自己的 Google Cloud 项目中：

1. 启用 Gmail API。
2. 配置 OAuth 同意屏幕和测试用户，测试用户为你要整理的 Gmail。
3. 新建类型为 **Web application** 的 OAuth 客户端。
4. 添加精确的授权重定向 URI：`https://你的云端域名/oauth/google/callback`。
5. 保存 Client ID 和 Client Secret 到服务器环境变量，不能放进手机源码。

请求权限为 `https://www.googleapis.com/auth/gmail.modify`。读取之外，`modify` 允许 App 在你手动点击时执行归档、标记已读、加星等操作；服务端不会主动改写邮件，删除邮件不在支持范围内。本项目会使用 offline access 与 PKCE，保存加密 refresh token，不持久保存访问令牌。

注意：从旧版 `gmail.readonly` 升级后，需要断开并在 App 中重新连接 Gmail 一次，让新权限生效；否则归档等操作会提示权限不足。

## 2.1 隐私与数据边界

- 云端以加密形式（Fernet）保存简报摘录、授权令牌和设置；不保存邮件全文，验证码类邮件不落库。
- 简报最多保留最近 30 期；附件仅在点击时临时经云端中转，不写入数据库。
- 数据不用于任何模型训练；本版不调用 ChatGPT 或其他 AI 摘要服务。开启翻译时，简报标题与摘录会发送至 Google Cloud Translation。
- App 内可一键“清空云端简报历史”，或“断开 Gmail 并清除云端简报”删除全部数据（含自定义分类规则）。
- 分享或录屏前，可在首页点“打码”隐藏账号、标题与正文。

## 2.2 自定义分类规则

App 邮件卡片上的“改分类”会为发件域名（或主题关键词）保存规则，后续整理优先生效；服务端接口为 `GET/POST /v1/rules` 与 `DELETE /v1/rules/{index}`，上限 100 条。

注意：Google 外部应用处于 Testing 状态并使用 Gmail 权限时，刷新令牌通常在7天后过期，需要重新授权。长期使用与公开发行需满足 Google 的发布及相应权限验证要求。把原来 ChatGPT 的授权复制过来不可行，也没有尝试绕过它。

参考：
- https://developers.google.com/identity/protocols/oauth2/web-server
- https://developers.google.com/identity/protocols/oauth2#expiration
- https://developers.google.com/workspace/gmail/api/reference/rest/v1/users.messages/list

## 3. 生成配置

在本目录终端执行：

```text
python -m venv .venv
```

Windows 接着执行：

```text
.venv\Scripts\python -m pip install -r requirements.lock
.venv\Scripts\python init_config.py
```

Linux/macOS：

```text
.venv/bin/pip install -r requirements.lock
.venv/bin/python init_config.py
```

`init_config.py` 会在本机生成 `.env`，含随机 APP_API_KEY 和 DATA_ENCRYPTION_KEY，不覆盖已有配置。编辑以下字段：

| 字段 | 用途 |
| --- | --- |
| PUBLIC_BASE_URL | 服务器 HTTPS 根地址，无末尾路径 |
| GOOGLE_CLIENT_ID | Google Web OAuth 客户端 ID |
| GOOGLE_CLIENT_SECRET | Google 客户端密钥，仅留在云端 |
| OWNER_EMAIL | 允许连接的唯一 Gmail 地址 |
| APP_API_KEY | 手机连接密钥，脚本生成 |
| DATA_ENCRYPTION_KEY | 数据库加密密钥，脚本生成并需妥善备份 |
| DATABASE_PATH | SQLite 持久路径，Docker 中为 /data/maildaily.sqlite3 |
| EXPO_ACCESS_TOKEN | 可选，与 Expo 增强推送访问控制配置配套 |

## 4. 启动后端

Python 方式（先激活虚拟环境或使用其解释器）：

```text
python -m uvicorn run:app --host 127.0.0.1 --port 8000 --workers 1 --no-access-log
```

Docker 方式，在已配置 `.env` 的本目录：

```text
docker compose up -d --build
```

Compose 把服务仅映射到宿主机 `127.0.0.1:8000`，由你已有的 HTTPS 反向代理或托管平台入口转发。需保留 `maildaily-data` 卷；不要执行会删除数据卷的清理操作。平台部署时把 HTTP 服务端口设为8000，挂载持久目录到 `/data`，配置同样的环境变量。

以支持 HTTPS 的反向代理转发到本机8000端口。保留 Authorization 请求头，超时至少180秒，关闭或脱敏 `/oauth/google/callback` 的查询日志，避免日志记录授权 code。`/health` 是公开健康检查，其余业务接口需连接密钥。OAuth 回调靠一次性 state 校验。

数据库值均加密，备份时同时备份数据库卷与 DATA_ENCRYPTION_KEY；丢失密钥无法解密历史。密钥轮换目前需显式数据迁移，不要直接更换加密密钥。不能运行多个后端副本共享此 SQLite 文件。

## 5. 手机接入

1. “云端”页填写 HTTPS 根地址与 APP_API_KEY。
2. 验证并保存后回到首页，点击 Google 连接。
3. 浏览器完成授权，返回 App 刷新状态。
4. “立即整理”首期读取过去24小时；之后从成功结束时间继续。
5. 在“云端”页设置时间，默认北京时间19:00。

服务每30秒检查一次时间，失败后约5分钟重试。恢复服务时，如果当日设定时间已过而任务未完成，会补做。处理开始时间与通知到达时间可能有偏差。手动整理也会在完成后通知已登记的设备。

## 6. 手机安装版与推送

先保持 Expo Go 作为界面试用环境。独立推送测试使用你自己的 Apple 开发者签名与推送凭证：

1. 在手机项目根目录登录 Expo CLI，运行 `npx eas-cli@latest init`，建立你自己的 EAS 项目。
2. 按 Expo 官方推送设置流程配置 Apple 的 APNs 凭证。
3. 如需要，修改 `app.json` 中 bundleIdentifier 为自己使用的唯一标识。
4. 执行 `npx eas-cli@latest build --platform ios --profile preview`，云端编译；Windows 不需要 Xcode。
5. 在登记的手机安装签名构建，进入“云端”页点“启用独立推送”。App 会使用 EAS 项目 ID 获取 ExpoPushToken 并登记到自己的后端。

服务器向 Expo 推送服务发送不含邮件正文的“简报已更新”通知，并检查 ticket 与 receipt。设备注销则移除 token，临时失败有限重试；推送服务受理不等于手机一定收到。需真机确认权限、签名、Apple凭证和最终投递。

此版本登记一个推送设备。再次登记会替换旧设备。想关闭推送可在 App 中删除登记。

参考：
- https://docs.expo.dev/push-notifications/push-notifications-setup/
- https://docs.expo.dev/develop/development-builds/introduction/
- https://docs.expo.dev/versions/v57.0.0/sdk/glass-effect/

## 7. 测试与范围

在 server 目录执行 `python -m unittest discover -s tests -v`。Google 和推送外网行为在测试中模拟，测试成功不代表真实授权或推送已完成。

本版是单人自用原型。无多用户注册、无 QQ/网易/Outlook/iCloud 实际连接、无 AI 改写服务。未来扩展为多人产品需要重做账号体系、每用户隔离、队列调度及相应 Google 权限合规流程。
